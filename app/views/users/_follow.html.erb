<%= form_for(current_user.following_relationships.build(following_id: @user.id)) do |f| %>
  <div><%= hidden_field_tag :following_id,@user.id %></div>
  <%= f.button :type => "フォローする", class:"follow-btn fa fa-user-plus" %>
<% end %>