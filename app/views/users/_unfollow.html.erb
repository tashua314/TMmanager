<%= form_for(current_user.following_relationships.find_by(following_id: @user.id),
            html: { method: :delete }) do |f| %>
  <%= f.button :type => "フォロー解除", class:"unfollow-btn fa fa-user-minus" %>
<% end %>
